# Captured `tools/list` dump

This file used to hold a full JSON `tools/list` payload. That dump goes stale.

Authoritative names, aliases, and parameters: [TOOLS_LIST.md](../../TOOLS_LIST.md). How-to: [USAGE.md](../../USAGE.md). Historical JSON remains in this folder for contract diffs.

Do not copy an advertised-count from these captures. Compute it from `src/agentdecompile_cli/registry.py`.
