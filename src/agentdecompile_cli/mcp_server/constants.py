"""Constants used across MCP server tool providers.

This module contains shared constants to avoid circular import issues.
"""

# Default timeout for decompilation operations (seconds)
DEFAULT_TIMEOUT_SECONDS = 60
